@* @model SchoolManagementSystem.Models.ViewModels.DashboardViewModel

@{
    ViewData["Title"] = "Dashboard";
}

<h2 class="mb-4 text-primary fw-bold">Principal Dashboard</h2>

<div class="row g-4">

    <!-- TOTAL USERS -->
    <div class="col-md-3">
        <div class="card shadow-sm border-0 text-center">
            <div class="card-body">
                <i class="bi bi-people-fill fs-1 text-primary"></i>
                <h5 class="mt-2">Total Users</h5>
                <h3 class="fw-bold">@Model.TotalUsers</h3>
            </div>
        </div>
    </div>

    <!-- PRINCIPALS -->
    <div class="col-md-3">
        <div class="card shadow-sm border-0 text-center">
            <div class="card-body">
                <i class="bi bi-person-badge fs-1 text-success"></i>
                <h5 class="mt-2">Principals</h5>
                <h3 class="fw-bold">@Model.TotalPrincipals</h3>
            </div>
        </div>
    </div>

    <!-- TEACHERS -->
    <div class="col-md-3">
        <div class="card shadow-sm border-0 text-center">
            <div class="card-body">
                <i class="bi bi-mortarboard fs-1 text-warning"></i>
                <h5 class="mt-2">Teachers</h5>
                <h3 class="fw-bold">@Model.TotalTeachers</h3>
            </div>
        </div>
    </div>

    <!-- STUDENTS -->
    <div class="col-md-3">
        <div class="card shadow-sm border-0 text-center">
            <div class="card-body">
                <i class="bi bi-person-lines-fill fs-1 text-danger"></i>
                <h5 class="mt-2">Students</h5>
                <h3 class="fw-bold">@Model.TotalStudents</h3>
            </div>
        </div>
    </div>

</div> *@
@{
    ViewData["Title"] = "Home";
}

@if (User.IsInRole("Teacher"))
{
    <div class="container mt-4">
        <h4 class="text-muted">
            Welcome! You can view your profile from the menu.
        </h4>
    </div>
}
